# 快速檢查工作流程：push 到 main 即在三平台跑 cargo check——跨平台編譯問題
# （cfg 邊界、平台專屬依賴、簽名漂移）在 push 時現形，不等 20 分鐘的 release。
# 教訓（2026-08-20 三連）：Windows 本機綠燈會掩蓋 linux/macOS 的 cfg 內程式碼錯誤。
name: check

on:
  push:
    branches: [main]
  pull_request:

jobs:
  cargo-check:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'ubuntu-22.04'
          - platform: 'macos-latest'
          - platform: 'windows-latest'
    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4
      - name: install Linux system dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf
      - uses: dtolnay/rust-toolchain@stable
      - uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'
      - name: cargo check（ocore/oserver/obridge——不含 Tauri 殼）
        # 排除 operoid（src-tauri）：其 build script 要求 externalBin 檔案已暫存
        # （release 的 beforeBuildCommand 產物），check 階段不存在；殼的平台
        # 問題由 release workflow 覆蓋。check 的價值在核心/服務/橋接的 cfg 邊界。
        run: cargo check --workspace --exclude operoid --all-targets
